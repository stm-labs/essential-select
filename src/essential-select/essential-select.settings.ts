/**
 * Container wrapper width for essential select
 */
export enum WrapperContent {

  // user for container css property "width: max-content"
  MAX_CONTENT = 1,

  // default value
  // user for container css property "width: auto"
  AUTO = 2,

  // dynamically change container width depends on input width section
  MATCH_FORM = 3

}
